name: 'test'
on: push
jobs:
  weather:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: replace file
        run: |
          git fetch
          git branch -a
