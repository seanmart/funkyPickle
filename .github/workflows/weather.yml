name: 'weather'
on:
  schedule:
    - cron: '15 0 * * *'
    - cron: '15 12 * * *'

jobs:
  weather:
    runs-on: ubuntu-latest
    steps:
      - with:
          node-version: "16"
      - run: 'actions/weather.js'
